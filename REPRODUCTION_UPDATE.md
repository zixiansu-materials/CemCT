# CemCT 全时段拟合修订记录

本次修改仅针对论文复现设置、结果导出和稿件中的对应方法描述。

## 复现配置和结果

随机游走拟合区间固定为 0.0–1.0，即全部 500 个保存时间点（步数 0–4990，stride 10）。使用 3000 walkers、5000 steps、deterministic_seed=True、单进程、三个方向贯通掩膜并集。六个分析模块已按最终 notebook 配置重新完成，结果与对应环境信息保存在 results/manuscript 中。

随机游走曲折度 X=4.9561404381、Y=5.6982488015、Z=4.6096686482，保留一位小数为 5.0、5.7、4.6，与稿件一致。此次没有扫描参数以寻找最佳匹配；全时段拟合是原 ZIP 中已有的一种做法，并已由用户确认用于修订后的复现包。该结果不是关于未保存历史设置的证明。

其余分析指标参见 manuscript_comparison.csv。该文件记录与稿件舍入数值的差值，不采用人为设定的通过阈值。

原源码未修改；此前对同一源码完成的 40 项自动测试全部通过。此次改动是配置和文档，六模块重算用于验证最终配置。原教学 notebook 保留自己的合成数据设置，不作为实验论文的复现入口。

## 稿件修改

CemCT_SoftwareX_V3.6.docx 保留原有图、公式和样式，完成四处定点替换：

1. 实验随机游走方法明确 PyTrax 的确定性种子选项、单进程和全部保存时间点的过原点拟合。
2. Table 3 的 square capillary 改为 cylindrical capillary。
3. 验证正文的 square-capillary 改为 cylindrical-capillary。
4. Fig. 2 图注明确 Hagen–Poiseuille 解对应圆柱毛细管。

稿件其他压缩包成员均与原稿逐字节一致；正文中的公式对象、图片对象、表格与节属性数量均保持不变。文本和结构检查通过。文档渲染器缺少 LibreOffice，本机 Word COM 渲染又因登录会话错误无法启动，因此尚未完成逐页视觉排版检查，提交前仍需在 Word 中检查修改段落的分页。

## 尚未处理的发布事项

本次未替用户指定数据许可、生成 DOI 或发布仓库，也未清理原始教学 notebook。README 原有发布状态与引用占位符等问题仍需另行整理。
